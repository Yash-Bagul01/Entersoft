import React from "react";
import ClosedLoopRemediationRoot from "@/components/platform/roots/ClosedLoopRemediationRoot";

export const metadata = {
  title: "Closed-Loop Remediation — EnProbe Platform | Entersoft Security",
  description: "Push actions into engineering and security workflows, verify fixes and write validated outcomes back to the risk record.",
};

export default function ClosedLoopRemediationPage() {
  return <ClosedLoopRemediationRoot />;
}
