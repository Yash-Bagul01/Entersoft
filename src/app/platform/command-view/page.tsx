import React from "react";
import CommandViewRoot from "@/components/platform/roots/CommandViewRoot";

export const metadata = {
  title: "Unified Command View — EnProbe Platform | Entersoft Security",
  description: "Give CISOs, operators and engineering leaders role-based views of exposure, incidents, controls, remediation and business impact.",
};

export default function CommandViewPage() {
  return <CommandViewRoot />;
}
