import React from "react";
import DataFusionRoot from "@/components/platform/roots/DataFusionRoot";

export const metadata = {
  title: "Security Data Fusion — EnProbe Platform | Entersoft Security",
  description: "Unify signals from AppSec, IAM, cloud, EDR, SIEM, threat intelligence, GRC and engineering systems.",
};

export default function DataFusionPage() {
  return <DataFusionRoot />;
}
