import React from "react";
import DastExoapePage from "@/components/platform/DastExoapePage";

export const metadata = {
  title: "DAST & AI DAST — EnProbe Platform | Entersoft Security",
  description: "Test runtime web applications and APIs like an attacker using Proof-Based Scanning with zero false positives.",
};

export default function DastPage() {
  return <DastExoapePage />;
}
